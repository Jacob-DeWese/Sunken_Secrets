using UnityEngine;

public class NPC_Tracking : MonoBehaviour
{
    /*
    Thinking this through: 
    - if (level == n)
    - list of NPCs (gameObjects) for that level
    - full interaction is forced on the player when collided, and when collided it first checks the tag is NPC_Required, then which GameObject
    */

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
